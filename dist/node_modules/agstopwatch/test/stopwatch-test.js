/// <reference path="definitions/mocha.d.ts" />
/// <reference path="definitions/node.d.ts" />
System.register(["../agstopwatch", "assert"], function(exports_1, context_1) {
    "use strict";
    var __moduleName = context_1 && context_1.id;
    var AGStopwatch, assert;
    return {
        setters:[
            function (AGStopwatch_1) {
                AGStopwatch = AGStopwatch_1;
            },
            function (assert_1) {
                assert = assert_1;
            }],
        execute: function() {
            describe("Stopwatch tests", function () {
                it("should run for 1 second", function () {
                    var sw = new AGStopwatch();
                    var start = sw.start();
                    while (Date.now() < start + 1000)
                        ;
                    sw.stop();
                    assert(sw.elapsed >= 1000);
                });
                it("should return 0 ms elapsed after instantation", function () {
                    var sw = new AGStopwatch();
                    assert(sw.elapsed === 0);
                });
                it("should return 0 ms elapsed after being reset", function () {
                    var sw = new AGStopwatch();
                    var start = sw.start();
                    while (Date.now() < start + 1000)
                        ;
                    sw.stop();
                    sw.reset();
                    assert(sw.elapsed === 0);
                });
                it("should return running = true while running and running = false while not running", function () {
                    var sw = new AGStopwatch();
                    assert(!sw.running);
                    sw.start();
                    assert(sw.running);
                    sw.stop();
                    assert(!sw.running);
                    sw.start();
                    assert(sw.running);
                    sw.restart();
                    assert(sw.running);
                    sw.reset();
                    assert(!sw.running);
                });
            });
        }
    }
});
//# sourceMappingURL=stopwatch-test.js.map